<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardUseCompatPadding="true"
    card_view:cardCornerRadius="0dp"
    card_view:cardBackgroundColor="@android:color/transparent"
    android:padding="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/top_item_bg">

        <!-- Icono opcional (puedes cambiar su tamaño, src y tint) -->
        <ImageView
            android:id="@+id/ivTopIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentStart="true"
            android:layout_marginStart="8dp"
            android:layout_centerVertical="true"
            android:src="@drawable/frog_main"
            card_view:tint="#00FF00" />

        <TextView
            android:id="@+id/tvTopInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/ivTopIcon"
            android:layout_marginStart="8dp"
            android:layout_centerVertical="true"
            android:text="Texto Info"
            android:textColor="#00FF00"
            android:textSize="12sp"
            android:fontFamily="@font/press_start_2p"/>
    </RelativeLayout>

</androidx.cardview.widget.CardView>